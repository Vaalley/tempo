# Build/config validation only. Replace these before starting any services.
# Use random hexadecimal passwords (no URL-special characters).
POSTGRES_PASSWORD=BUILD_ONLY_REPLACE_BEFORE_START
MONGO_PASSWORD=BUILD_ONLY_REPLACE_BEFORE_START
JWT_SECRET=BUILD_ONLY_REPLACE_BEFORE_START_WITH_RANDOM_SECRET
TEMPO_ORIGIN=http://localhost:18080
TEMPO_PORT=18080
