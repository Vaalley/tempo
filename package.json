{
	"name": "tempo",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "bun --filter '*' dev",
		"build": "bun --filter '*' build",
		"lint": "oxlint . --ignore-path .gitignore",
		"format": "oxfmt .",
		"format:check": "oxfmt . --check",
		"test": "bun --filter '*' test",
		"precommit": "bun run format && bun run lint && bun run test",
		"diagrams:png": "bun run scripts/export-diagrams.ts"
	},
	"devDependencies": {
		"marked": "^18.0.5",
		"oxfmt": "^0.21.0",
		"oxlint": "^1.68.0",
		"puppeteer": "^25.1.0"
	}
}
