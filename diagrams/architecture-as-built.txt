@startuml

title Tempo - Architecture 3-tiers implémentée (V1)
left to right direction

skinparam backgroundColor #FFFFFF
skinparam shadowing false
skinparam roundCorner 12
skinparam componentStyle rectangle
skinparam packageStyle rectangle
skinparam ArrowColor #475569
skinparam DefaultFontName Arial

actor "Collaborateur ou\nadministrateur" as User

rectangle "Couche présentation\nSvelteKit 2.63.1 / Svelte 5.56.3" as Presentation #E0F2FE {
    component "Pages SvelteKit\n/, /login, /bookings, /check-in, /admin/*" as Pages
    component "Gardes de navigation\net client Hono RPC typé" as Client
    component "Jeton JWT\ndans localStorage" as Token
    Pages --> Client
    Token --> Client
}

rectangle "Couche métier et API\nBun 1.3.14 / Hono 4.12.24" as ApiTier #EDE9FE {
    component "CORS, en-têtes de sécurité,\nrate limiting et gardes JWT/ADMIN" as Security
    component "Routes HTTP\nauth, users, workspaces, bookings,\nparticipants, check-in, analytics, audit" as Routes
    component "Services métier" as Services
    Security --> Routes
    Routes --> Services : appels typés
}

rectangle "Couche persistance" as Persistence #DCFCE7 {
    database "PostgreSQL 18.6\nutilisateurs, espaces, réservations,\nparticipants et jetons QR hachés" as PostgreSQL
    database "MongoDB 8.0.29\nlogs de suppression" as MongoDB
}

User --> Pages : navigateur
Client --> Security : HTTP JSON\nAuthorization: Bearer JWT
Services --> PostgreSQL : Drizzle ORM / SQL
Services --> MongoDB : pilote MongoDB\naudit best effort

note bottom of Persistence
  Architecture observée dans le code de la V1.
  Aucune couche Repository distincte.
end note

@enduml
